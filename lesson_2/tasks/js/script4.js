// ====================task-1====================================================================================================================================

// Ввод данных
const sm = parseFloat(prompt("Введите длинну в см", "0"));

// Выражение №1
const km = sm * 0.00001;

// Вывод данных
document.getElementById("km").innerHTML = km;

document.getElementById("sm").innerHTML = sm;
